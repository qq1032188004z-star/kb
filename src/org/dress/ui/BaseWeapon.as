package org.dress.ui
{
   public class BaseWeapon extends BaseDecoration
   {
      
      public function BaseWeapon(rowCount:int = 4, decorationType:int = 4)
      {
         super(rowCount,decorationType);
      }
   }
}

